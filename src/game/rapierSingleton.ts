/**
 * This file only allows the import of a single Rapier
 * You're not allowed to have multiple imports of three or Rapier, so
 * we make a singleton of it that gets called everywhere
 * */
import * as Rapier from "@dimforge/rapier3d";
export default Rapier;
