/**
 * This file only allows the import of a single Three
 * You're not allowed to have multiple imports of three or Three, so
 * we make a singleton of it that gets called everywhere
 * */
import * as Three from "Three";
export default Three;
